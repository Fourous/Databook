void buy(){
	int x,y,z;
	int count=0;
	for ( x = 0; i < 20; ++x)
	{
		for (int y= 0; y <33 ; ++y)
		{
			if (x+y+z=100&&5*x+3*y+z/3=100)
			{
				count++;
				printf("%d,%d,%d\n",x,y,z );
			}
			
		}
	}
	if (count==0)
			{
				printf("this is no answer!\n");
			}
}